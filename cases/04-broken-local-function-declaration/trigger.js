// Stands in for any module level binding that happens to carry the same name.
// In the report this role was played by an import, `TransitionGroup as o`.
export function getActivator() { return 'THE OTHER ONE' }
