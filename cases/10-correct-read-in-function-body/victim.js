// Only difference from case 1: n is read in the function body, not in a default value.
export function factory(get) {
  const read = function (a) { return a || n.IS_BLOCK }
  var n = get(81937)
  return read
}
